#include "main.h"

/**
 * add - adds two integers and returns the result
 * @u: int to be added to v
 * @v: int to be added to u
 * Return: sum of a and b
 */
int add(int u, int v)
{
	int w;

	w = u + v;

	return (w);
}
